import React from 'react'

/**
 * Define NotFound as a 'dumb' Component.
 * Because the Component doesn't have any built in logic,
 * it is only defined as a function which returns JSX.
 */
const NotFound = () => (
    <h1>404 not found</h1>
)

export default NotFound
